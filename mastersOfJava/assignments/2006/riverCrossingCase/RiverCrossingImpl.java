/**
 * RiverCrossingImpl
 */

public class RiverCrossingImpl {

	/**
	 * Calculates the time that N persons need to cross a bridge.
	 * - If there are persons on the bridge they need a bottle of Jenever.
	 * - Only two persons may be on the bridge at once.
	 * - The time to cross the bridge by group of persons on the bridge 
	 *   is determined by the slowest person.
	 * - The time to cross the bridge of a person is determined by its number. 
	 * @param n the number of persons.
	 */ 
	public int getTime(int n) {
		// 
		// Implement here
		//
		return 0;
	}
	
}
