package com.gnorsilva.moj._0_easypeasy.spotcountercase;

public interface Hit {

	/**
	 * returns true if the hit is a SPOT, false if not.
	 * @return true if the hit is a SPOT, false if not.
	 */
	boolean isSpot();
	
}
