/**
 * ColorToolkit contains Dr.Wongs utilities for seeing what
 * the client sees.
 */

public class ColorToolkit {
	
	/**
	 * makeGrayscale creates a new grayscaled Bitmap from the given Bitmap.  
	 * @param bmp the Bitmap to make a grayscaled bitmap from.  
	 * @return a blurred bitmap. 
	 */
	public Bitmap makeGrayscale(Bitmap bmp) {
		//
		return bmp.copy();
		//
	}
	
	/**
	 * makeBlurred creates a new blurred Bitmap from the given Bitmap. 
	 * @param bmp the Bitmap to make a blurred bitmap from.
	 * @return a blurred bitmap. 
	 */
	public Bitmap makeBlurred(Bitmap bmp) {
		//
		return bmp.copy();
		//
	}
	
}
