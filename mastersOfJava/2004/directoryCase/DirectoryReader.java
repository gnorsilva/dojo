/**
* Reads a Directory and performs transformations on it
*/
public class DirectoryReader {
	/**
	* Reads a Directory object and returns its flat representation as
	* an array of lines. Each lines contains the full pathname of a file
	* (not the directories by themselves), including the root. 
	* Eg: "/dira/dirb/text".
	* @param dir the directory which is read and "flattened" to a series
	* of text lines
	* @return an array of String objects. Each String contains a file
	* name with its full path (no directories by themselves!)
	* @throws DirectoryException thrown when illegal input has been passed
	*/
	public String[] readDirectory(Directory dir) throws DirectoryException {
		// IMPLEMENT THIS
		return null;
	}
}
